# Home Assistant

This directory will contain importable examples for:

- Receiving the ESPHome doorbell event
- Capturing a camera snapshot locally
- Sending a notification with the snapshot
- Optional dashboard camera and doorbell-status cards
- Optional unavailable-device alert

Entity IDs, notification targets, storage paths, and URLs will use placeholders. No private Home Assistant configuration belongs in this public repository.
